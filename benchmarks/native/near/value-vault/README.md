# Native NEAR ValueVault

Primary source: [`testkit/compare/near/value-vault`](../../../../testkit/compare/near/value-vault)

```sh
just near-compare-value-vault
cargo test --manifest-path testkit/compare/near/value-vault/Cargo.toml --features host-tests
```
