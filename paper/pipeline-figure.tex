\documentclass[tikz,border=2pt]{standalone}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning, fit, calc, backgrounds}

\begin{document}

\tikzset{
  block/.style={
    rectangle, rounded corners=2pt, draw=blue!50!black, fill=blue!8,
    minimum height=7mm, minimum width=22mm, font=\small\sffamily,
    inner sep=4pt, align=center, line width=0.5pt
  },
  coreblock/.style={
    rectangle, rounded corners=2pt, draw=teal!60!black, fill=teal!10,
    minimum height=7mm, minimum width=30mm, font=\small\sffamily,
    inner sep=4pt, align=center, line width=0.5pt
  },
  targetblock/.style={
    rectangle, rounded corners=2pt, draw=orange!70!black, fill=orange!10,
    minimum height=6mm, minimum width=20mm, font=\footnotesize\sffamily,
    inner sep=3pt, align=center, line width=0.5pt
  },
  artifactblock/.style={
    rectangle, rounded corners=2pt, draw=gray!60!black, fill=gray!8,
    minimum height=6mm, minimum width=20mm, font=\footnotesize\sffamily,
    inner sep=3pt, align=center, line width=0.5pt
  },
  gate/.style={
    diamond, draw=red!60!black, fill=red!8, aspect=2,
    minimum height=8mm, font=\footnotesize\sffamily,
    inner sep=2pt, align=center, line width=0.5pt
  },
  arrow/.style={-Stealth, thick, draw=blue!50!black},
  bidir/.style={Stealth-Stealth, thick, draw=gray!60!black, dashed},
}

\begin{tikzpicture}[node distance=8mm and 12mm]

  %% Layer 1: Authoring
  \node[block] (sdk) {Lean SDK\\\scriptsize Contract Intent API};
  \node[block, right=of sdk] (tok) {Token SDK\\\scriptsize TokenSpec};

  %% Layer 2: ContractSpec
  \node[coreblock, below=12mm of sdk, xshift=12mm] (spec) {ContractSpec};

  %% Layer 3: Portable IR
  \node[coreblock, below=of spec] (ir) {Portable Contract IR\\\scriptsize + AllocatorConfig + Ownership};

  %% Layer 4: Targets
  \node[targetblock, below left=10mm and 5mm of ir] (evm) {EVM\\\scriptsize Plan $\to$ Yul};
  \node[targetblock, below=10mm of ir] (sol) {Solana\\\scriptsize sBPF Asm};
  \node[targetblock, below right=10mm and 5mm of ir] (near) {NEAR/Wasm\\\scriptsize EmitWat $\to$ WAT};

  %% Artifacts
  \node[artifactblock, below=5mm of evm] (evmout) {Bytecode + ABI};
  \node[artifactblock, below=5mm of sol] (solout) {ELF + manifest};
  \node[artifactblock, below=5mm of near] (nearout) {.wasm + metadata};

  %% Capability gate
  \node[gate, left=14mm of ir] (cap) {Cap.\\Check};

  %% Arrows
  \draw[arrow] (sdk) -- (spec);
  \draw[arrow] (tok) -- (spec);
  \draw[arrow] (spec) -- (ir);
  \draw[arrow] (ir) -- (evm);
  \draw[arrow] (ir) -- (sol);
  \draw[arrow] (ir) -- (near);
  \draw[arrow] (evm) -- (evmout);
  \draw[arrow] (sol) -- (solout);
  \draw[arrow] (near) -- (nearout);
  \draw[arrow] (ir) -- (cap);
  \draw[bidir] (cap) -- (spec);

\end{tikzpicture}

\end{document}