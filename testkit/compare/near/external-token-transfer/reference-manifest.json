{
  "schema": "proof-forge.near.reference-equivalence.v0",
  "framework": "near-sdk-rs",
  "program": "external-token-transfer",
  "source": "src/lib.rs",
  "peer": "peer/",
  "proofForgeSource": "Examples/Product/ExternalTokenTransfer.lean",
  "proofForgeTarget": "wasm-near",
  "proofForgeModule": "ExternalTokenTransfer",
  "scenario": {
    "offline": ["initialize", "pay → promise ft_transfer on usdc.peer"],
    "live": ["deploy FT peer", "rebuild PF with --peer usdc.peer=<id>", "initialize + pay"]
  },
  "honesty": [
    "Protocol client (Layer B): calls peer NEP-141, not a full FT body.",
    "PF packs NEP-141 JSON; receiver_id resolution for u64 recipients may be empty offline.",
    "sdk uses synthetic receiver_id strings from u64 tags; mock peer ignores receiver validity.",
    "SoulboundToken/ERC4626Vault not in matrix (TokenSpec / stdlib build gap)."
  ]
}
