{
  "schema": "proof-forge.near.reference-equivalence.v0",
  "program": "height-lock-vault",
  "source": "src/lib.rs",
  "proofForgeSource": "Examples/Product/HeightLockVault.lean",
  "proofForgeTarget": "wasm-near",
  "proofForgeModule": "HeightLockVault",
  "scenario": {
    "steps": [
      { "call": "init" },
      { "call": "lock", "amount": 1000, "unlockHeight": 1 },
      { "call": "claim" },
      { "view": "claim_balance", "expect": 1000 },
      { "view": "is_claimed", "expect": 1 },
      { "view": "get_locked", "expect": 0 }
    ]
  },
  "honesty": [
    "Binary height lock (block_height/checkpointId >= unlockHeight).",
    "Not wall-clock TimelockVault; not linear VestingVault.",
    "Internal claim ledger only — no external token transfer.",
    "Live: unlockHeight=1 unlocks under any real sandbox height."
  ]
}
