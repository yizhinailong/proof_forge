{
  "schema": "proof-forge.near.reference-equivalence.v0",
  "framework": "near-sdk-rs",
  "program": "storage-deposit",
  "source": "src/lib.rs",
  "upstream": {
    "name": "NEP-145 storage management (minimal)",
    "notes": "U64 projected balances with partial withdraw; no StorageBalance JSON, unregister, or predecessor refund."
  },
  "proofForgeSource": "Examples/Product/StorageDeposit.lean",
  "proofForgeTarget": "wasm-near",
  "proofForgeModule": "StorageDeposit",
  "scenario": {
    "steps": [
      { "call": "init" },
      { "call": "storage_balance_bounds", "expect": 1 },
      { "call": "storage_balance_of", "account": "alice", "expect": 0 },
      { "call": "storage_deposit", "account": "alice", "attached_deposit": 7 },
      { "call": "storage_balance_of", "account": "alice", "expect": 7 },
      { "call": "storage_withdraw", "account": "alice", "amount": 3 },
      { "call": "storage_balance_of", "account": "alice", "expect": 4 }
    ]
  },
  "honesty": [
    "NEP-145-lite: cumulative U64 deposit map, not full JSON StorageBalance.",
    "PF keys by 32-byte account hash; sdk uses AccountId LookupMap.",
    "Attached deposit drives storage_deposit via nativeValue / env::attached_deposit.",
    "storage_withdraw is caller-bound ledger debit only; no predecessor refund Promise is emitted yet."
  ]
}
