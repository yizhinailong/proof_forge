[scenario]
name = "allocator-release"
fixture = "alloc-release"
targets = ["wasm-near"]
capabilities = ["data.fixed_array"]

[[step]]
call = "release_then_sum"
[step.expect.return]
u64 = 60
[step.expect]
allocations = 2
reuses = 1
deallocations = 1
