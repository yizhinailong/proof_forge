[scenario]
name = "unsupported-crosscall"
fixture = "unsupported-crosscall"
targets = ["solana-sbpf-asm"]
capabilities = ["crosscall.invoke"]

[[diagnostic]]
target = "solana-sbpf-asm"
# Portable crosscall.invoke is supported (CPI); empty peer fails closed via PortableHonesty.
name = "crosscall.invoke unsupported"
contains = [
  "PortableHonesty Crosscall: Solana portable remote requires a non-empty peer id",
]
